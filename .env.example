# PostgreSQL Database URL (required for persistent data on Vercel)
# Format: postgresql://user:password@host:5432/dbname
# Get this from Vercel Postgres dashboard
POSTGRES_URL=postgresql://user:password@host:5432/dbname

# Alternative variable name (Vercel Postgres uses this by default)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Note: If neither POSTGRES_URL nor DATABASE_URL is set,
# the app will use SQLite in /tmp (data is ephemeral on Vercel)
